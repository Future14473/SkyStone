package org.futurerobotics.botsystem.ftc

import com.qualcomm.robotcore.eventloop.opmode.OpMode
import org.futurerobotics.botsystem.BaseElement

/**
 * An element which simply contains an [opMode].
 *
 * This does not have a default.
 */
class OpModeElement(val opMode: OpMode) : BaseElement()
